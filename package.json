{
  "name": "work-ledger-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check:static": "node scripts/check-site.mjs",
    "check:browser": "node scripts/browser-smoke.mjs",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1 --directory site",
    "test": "npm run check:static && npm run check:browser"
  },
  "devDependencies": {
    "axe-core": "4.12.1",
    "playwright": "1.61.1"
  }
}
